     #include<stdio.h>
		int main(){
		int evennumbers =0;

		/*while (evennumbers <= 30){
				if (evennumbers % 2 == 0)
					printf("\n %d", evennumbers);
					evennumbers++;
				}

					return 0;
				}*/
		for (evennumbers = 0;evennumbers <= 30;evennumbers++){
				if (evennumbers % 2 == 0)
						printf("\n %d", evennumbers);
				}
					return 0;
				}
