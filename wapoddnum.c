#include<stdio.h>
		int main() {
		int oddnum = 0;

		/*while (oddnum <= 30){
			if (oddnum % 2 != 0)
				printf("\n %d", oddnum);
				oddnum++;
			}
				return 0;
			}*/
		for (oddnum = 0;oddnum <= 30;oddnum++){
				if (oddnum % 2 != 0)
					printf("\n %d ", oddnum);
			}
				return 0;
			}