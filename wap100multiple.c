#include<stdio.h>
		int main() { 
		int mul;
		int n = 0;

			printf("\nEnter a generic number :");
			scanf("%d",&mul);
			n = mul;


		/*while (mul <= 100){
			if (mul % n == 0)
			printf("\n%d",mul);
			mul++;
		}
			return 0;
		}*/

		for (mul = 0;mul <= 100;mul++){
			if (mul % n == 0)
				printf("\n%d", mul);
			}
				return 0;
			}